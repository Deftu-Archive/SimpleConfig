plugins {
    id 'java'
}

group project.lib_group
version project.lib_version

repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

dependencies {
    compile("${project.deps_jsontgm_group}:${project.deps_jsontgm_name}:${project.deps_jsontgm_version}")
}