plugins {
    id 'java'
}

group = project.lib_group
version = project.lib_version

repositories {
    mavenCentral()
    maven {
        name = "JitPack"
        url = "https://jitpack.io"
    }
}

dependencies {
    compile('xyz.matthewtgm:JsonTGM:2.6.3')
}